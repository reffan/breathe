import { Scenes } from '@/types'

const scenes: { name: string; scene: Scenes }[] = [
  // DEBUG:
  {
    name: 'DEBUG',
    scene: 'debug',
  },
  {
    name: 'Aqua',
    scene: 'aqua',
  },
]

export { scenes }
